{
  "name": "unical",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --test test/index.test.ts && wrangler deploy --dry-run"
  },
  "dependencies": {
    "ical.js": "^2.2.1"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
